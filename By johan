-- UserId Whitelist Protection
local whitelist = {
    12345678,
    23456789,
    34567890,
}

game.Players.PlayerAdded:Connect(function(player)
    if not table.find(whitelist, player.UserId) then
        player:Kick("You are not whitelisted.")
    end
end)